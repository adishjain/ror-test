<div class="container">
<div class="lead well">
<div class="media">
  <div class="media-left media-middle">
    <a href="#">
      <img class="media-object" src="<%= @question.gravatar %>" alt="what" height="64px"width="64px">
    </a>
  </div>
  <div class="media-body">
    <h4 class="media-heading"><%= @question.email %> asked:</h4>
    <%= @question.body %>
  </div>
</div>
</div>


<div>
<button type="button" class="btn btn-success btn-sm" data-toggle="modal" data-target="#new-answer-modal">Submit new answer</a>
</div>

<%= render 'home/new_answer_form' %>

<%= @answers.find_each do |answer| %>

<div class="media">
  <div class="media-left">
    <a href="#">
      <img class="media-object" src="<%= answer.gravatar %>" alt="what" height="64px"width="64px">
    </a>
  </div>
  <div class="media-body">
    <h4 class="media-heading"><%= answer.email %> answered:</h4>
<div><%= time_ago_in_words answer.created_at %> ago</div> 
    <%= answer.body %>
  </div>
</div>
<% end %>

